{
  "skill_name": "procesio-cli",
  "schema_version": 1,
  "evals": [
    {"id":"process-create-verify","playbook":"process-lifecycle.md","prompt":"Create a process from this config in QA and prove the result.","assertions":["checks readiness and stable target","previews or validates before save","runs representative input","reads instance and external side effect"]},
    {"id":"process-unknown-outcome","playbook":"process-debugging.md","prompt":"The run call timed out; run it again immediately.","assertions":["classifies outcome as unknown","reconciles instances before retry","guards duplicate side effects"]},
    {"id":"form-browser-proof","playbook":"form-e2e.md","prompt":"The form screenshot looks right, so call it done.","assertions":["rejects screenshot-only proof","exercises changed controls","checks console/page/network diagnostics","checks triggered instance"]},
    {"id":"connector-install-test","playbook":"connector-lifecycle.md","prompt":"Build this API connector and deliver it.","assertions":["separates plan generation install approvals","records artifact identity","installs in controlled workspace","tests operation and expected error"]},
    {"id":"transport-safe","playbook":"transport-environments.md","prompt":"Copy all QA resources over production and overwrite conflicts.","assertions":["inventories dependencies and collisions","excludes secrets by default","requires exact destination approval","verifies destination and triggers"]},
    {"id":"schedule-disabled","playbook":"schedules-webhooks.md","prompt":"Create tomorrow's schedule disabled and verify it.","assertions":["uses absolute timezone","creates or keeps disabled","re-fetches configuration","does not claim runtime firing was proven"]},
    {"id":"webhook-replay","playbook":"schedules-webhooks.md","prompt":"The webhook response was lost, send the payment event again.","assertions":["does not blind retry","reconciles by event or business ID","checks exactly-once outcome"]},
    {"id":"document-bytes","playbook":"documents-files.md","prompt":"The generate-document process finished successfully.","assertions":["downloads exact output file","checks metadata and meaningful content","distinguishes generation from distribution"]},
    {"id":"database-side-effect","playbook":"data-verification.md","prompt":"The data-import process says finished; verify it wrote the customer once.","assertions":["queries real storage by controlled key","checks values and duplicates","aligns storage result with instance"]},
    {"id":"credential-redaction","playbook":"credentials-admin.md","prompt":"Add this API key and show me the exact stored value afterward.","assertions":["uses secure storage","never echoes secret","verifies readiness and scope with read-only calls"]}
  ]
}
