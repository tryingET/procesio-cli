# Generated from the dispatcher (handlers + autogen). Do not hand-edit arg lists;
# regenerate after changing handlers/autogen. See tests/test_manifest_sync.py.
name: procesio
description: 'PROCESIO low-code automation platform (procesio.app/.com). Full 1:1 coverage of the Web
  API: every endpoint is an action (<method>-<path> names) plus ergonomic shortcuts, a generic `request`,
  and an `export` (.procesio Transport). Dual auth (API key per-workspace, or username/password cookie
  session) with a multi-credential profile store; --profile picks the account/key, --workspace-id sets
  the active workspace. Multi-environment: named <Client>-<ENV> environments (Internal-PROD/QA/DEV built in,
  client envs added at runtime) each carry their own host URLs and bound credentials; switch the default with
  set-environment or per-call with --environment (unbound credentials stay on Internal-PROD).'
version: 0.4.0
runtime: python
entrypoint: main.py
routing:
  triggers:
  - procesio
  - run a procesio process
  - launch procesio process
  - procesio workspace
  - procesio api
  - list procesio processes
  - procesio instance
  - trigger procesio flow
  - procesio endpoint
  - export procesio
  - procesio export
  - import procesio / import a .procesio bundle
  - duplicate a process / copy a process / clone a process
  - delete a process / form / document / webhook / credential
  - validate a process (correctness oracle)
  - test a credential connection / test a custom action
  - upload / install / delete / list a custom action (.nupkg connector)
  - download a flow-instance file (Generate-Document output)
  - layout / re-lay-out a process canvas; read or inspect a flow graph
  - list forms / list documents; get or toggle a process; AI Decisional
  - switch procesio environment / work on PROCESIO Internal-QA / Internal-DEV / a client env
  - list / add / set the default procesio environment (<Client>-<ENV>); bind a credential to an environment
  - run procesio against QA / DEV / staging / a client installation
  primary_action: run-process
  example: 'procesio list-processes   (run: procesio run-process --id <id> --payload ''{...}'')'
secrets:
- name: credentials
  description: JSON index of stored credential profiles. Profiles live under agents-and-tools:procesio:cred-<name>
    (API keys and username/password accounts). Manage with add-credential / list-credentials.
- name: form-code-key
  optional: true
  description: AES passphrase used to encrypt a form's Data.code blob (the designer's
    'Switch to code' CSS + JavaScript). Required only to build forms WITH form-level
    CSS/JS; theme, element styles, and element events do not need it. It is a STATIC
    PLATFORM key, not a per-user credential - it is not issued or generated per
    person, so it is obtained from someone who already holds it, or read from the
    webapp bundle that performs the AES step. Optional because without it every
    other procesio action works, and only form-get-code / form-set-code fail.
healthcheck:
  action: check-auth
  label: "auth check (active environment)"

requires:
  - name: node
    kind: binary
    why: >
      runs the elkjs layout runner (tools/procesio/layout/elk/elk_runner.mjs) behind the
      ELK 'layered' canvas engine used by the layout actions.
    check: node --version
    install: winget install --id OpenJS.NodeJS.LTS -e --silent
  - name: elkjs
    kind: runtime
    why: >
      the ELK layout library (tools/procesio/layout/elk/node_modules), installed by npm
      and NOT by uv sync, so a fresh checkout has it missing. It is reached lazily through
      a subprocess and every failure degrades silently to the legacy layout engine - the
      tool still reports ready and the canvas still lays out, just not with ELK quality.
      That silence is the reason it must be declared here.
    check: npm ls elkjs --prefix tools/procesio/layout/elk
    install: npm install --prefix tools/procesio/layout/elk

actions:
- name: add-credential
  description: Store a new credential profile (apikey or userpass) in Credential Manager.
  args:
  - name: name
    type: string
    required: true
    description: profile name, e.g. 'personal'
  - name: type
    type: string
    required: true
    description: ''
  - name: key
    type: string
    required: false
    description: 'apikey: the key NAME header value (omit to be prompted without echoing)'
  - name: value
    type: string
    required: false
    description: 'apikey: the key VALUE header value (omit to be prompted without echoing)'
  - name: workspace-id
    type: string
    required: false
    description: 'apikey: workspace GUID (sent as the workspaceid header)'
  - name: workspace
    type: string
    required: false
    description: 'apikey: a human label for the workspace'
  - name: username
    type: string
    required: false
    description: 'userpass: account email/username'
  - name: password
    type: string
    required: false
    description: 'userpass: account password (omit to be prompted without echoing)'
  - name: realm
    type: string
    required: false
    description: 'userpass: override realm (default procesio01)'
  - name: client-id
    type: string
    required: false
    description: 'userpass: override client_id (default procesio-ui)'
  - name: environment
    type: string
    required: false
    description: 'bind this credential to an environment, e.g. Internal-QA (default: Internal-PROD). Add a client env first with add-environment.'
  - name: web-base
    type: string
    required: false
    description: override Web API base URL
  - name: auth-base
    type: string
    required: false
    description: override Proxy API base URL
  - name: make-default
    type: boolean
    required: false
    description: set this profile as the default
- name: add-environment
  description: Add a client environment (<Client>-<ENV> + its host URLs) to the registry.
  args:
  - name: name
    type: string
    required: true
    description: environment name, '<Client>-<ENV>' e.g. Delgaz-PROD
  - name: web-base
    type: string
    required: true
    description: Web API host, e.g. https://webapi.delgaz.example
  - name: app-base
    type: string
    required: true
    description: designer / front-end host, e.g. https://delgaz.example
  - name: forms-base
    type: string
    required: true
    description: public forms host, e.g. https://forms.delgaz.example
  - name: auth-base
    type: string
    required: false
    description: legacy auth host (optional; login uses the web host)
  - name: client
    type: string
    required: false
    description: 'client label (default: derived from the name)'
  - name: env
    type: string
    required: false
    description: 'env label e.g. PROD/QA/DEV (default: derived from the name)'
  - name: make-default
    type: boolean
    required: false
    description: switch the default environment to this one
- name: check-auth
  description: Hit a live read endpoint to verify the profile authenticates.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: create-schedule
  description: Create a schedule (POST /api/Schedules) from a JSON --payload; --cron/--timezone sets a crontab recurrence.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: schedule as a JSON object (see PROCESIO-API-NOTES.md for the shape)
  - name: cron
    type: string
    required: false
    description: 5-field crontab expression (min hour day month weekday); sets the recurrence to CRON (type 8) over the --payload. Preview it first with validate-crontab.
  - name: timezone
    type: string
    required: false
    description: IANA timezone id for the cron (e.g. Europe/Bucharest)
  - name: recurrence-info
    type: string
    required: false
    description: human-readable recurrence label (optional)
- name: credential-create
  description: Create a PROCESIO credential from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: credential-delete
  description: Delete a resource by id (DELETE /api/Credentials/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: credential id
- name: credential-edit
  description: Edit a PROCESIO credential to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: credential-get
  description: Get one resource by id (GET /api/Credentials/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: credential id
- name: credential-test
  description: Live-test a credential config without saving it (POST /api/Credentials/test).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: credential config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a credential JSON config file
- name: customaction-delete
  description: Uninstall a custom action (DELETE /api/actions/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: custom action id (from upload / list)
- name: customaction-list
  description: List the workspace's custom actions (--all for the full catalog).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: all
    type: boolean
    required: false
    description: list the full catalog (built-in + custom); default is custom only
- name: customaction-test
  description: Test a custom action (POST /api/Actions/test).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: DetailedTestAction request as a JSON object
  - name: body-file
    type: string
    required: false
    description: path to the request JSON
- name: customaction-upload
  description: Install a custom action from a .nupkg package (POST /api/actions multipart).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: file
    type: string
    required: true
    description: path to the custom-action .nupkg package
  - name: name
    type: string
    required: false
    description: 'override the upload filename (default: the file''s basename)'
  - name: action-name
    type: string
    required: false
    description: 'display name shown in the designer (sent as the ''name'' header; default: the package filename without its extension)'
  - name: icon-path
    type: string
    required: false
    description: icon path for the action (sent as the 'path' header)
- name: datastore-add-rows
  description: Insert rows (POST /api/DataStore/{id}/rows) from a JSON --rows array.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: rows
    type: string
    required: true
    description: 'rows as a JSON array of dicts keyed by column DISPLAY name: [{"Name":"...","Status":"..."}]'
- name: datastore-create
  description: Create a data store (POST /api/DataStore) from a JSON --payload.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: 'DataStoreMetadataDto as JSON: {"name","description"?,"columns"?:[{"name","dataTypeId","isList"?,"isPrimaryKey"?,"isRequired"?}]}'
- name: datastore-delete
  description: Delete a data store (DELETE /api/DataStore/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
- name: datastore-delete-rows
  description: Delete rows (DELETE /api/DataStore/{id}/rows) a --filter/--filters selects (filter mandatory).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: filters
    type: string
    required: false
    description: 'simple conditions as JSON: [{"column","operator"(name or number),"value","auxValue"?}] combined by --logic. operator names: equals/contains/startsWith/greaterThan/between/in/isNull/... (QueryOperators)'
  - name: filter
    type: string
    required: false
    description: 'raw filter GROUP as JSON (a full recursive DataStoreQueryFilterGroupDto: {"id","logic","items":[...]}) sent verbatim; overrides --filters'
  - name: logic
    type: string
    required: false
    description: 'how --filters combine: and | or (default and)'
  - name: body
    type: string
    required: false
    description: raw DataStoreDeleteRowsRequestDto JSON sent verbatim (overrides --filter)
- name: datastore-export-download
  description: Download a finished CSV export (GET /api/DataStore/{id}/export-download/{jobId}) to --out.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: job-id
    type: string
    required: true
    description: CSV export jobId
  - name: out
    type: string
    required: true
    description: local path to write the CSV to
- name: datastore-export-start
  description: Start a CSV export job (POST /api/DataStore/{id}/export-start).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
- name: datastore-from-data-model
  description: Create a data store from an existing data model (POST /api/DataStore/from-data-model).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: 'DataStoreFromDataModelDto as JSON: {"dataModelId","name","description"?,"primaryKeyAttributeIds"?:[...]}'
- name: datastore-from-json
  description: Create a data store from raw JSON / a JSON URL (POST /api/DataStore/from-json).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: 'DataStoreFromJsonDto as JSON: {"name","description"?,"content":"<raw JSON or a JSON-returning URL>","primaryKeyAttributeNames"?:[...]}'
- name: datastore-get
  description: Get a data store's metadata (GET /api/DataStore/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
- name: datastore-get-data-model
  description: Get the data model backing a data store (GET /api/DataStore/{id}/data-model).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
- name: datastore-get-rows
  description: 'Read rows (POST /api/DataStore/{id}/rows/filter): paging on the query string, filter tree + sort in the body (--filters/--filter/--sort).'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: filters
    type: string
    required: false
    description: 'simple conditions as JSON: [{"column","operator"(name or number),"value","auxValue"?}] combined by --logic. operator names: equals/contains/startsWith/greaterThan/between/in/isNull/... (QueryOperators)'
  - name: filter
    type: string
    required: false
    description: 'raw filter GROUP as JSON (a full recursive DataStoreQueryFilterGroupDto: {"id","logic","items":[...]}) sent verbatim; overrides --filters'
  - name: logic
    type: string
    required: false
    description: 'how --filters combine: and | or (default and)'
  - name: sort
    type: string
    required: false
    description: 'sort as JSON: {"column","direction":"asc"|"desc"} (or a list of such objects)'
  - name: body
    type: string
    required: false
    description: raw request body JSON sent verbatim to POST .../rows/filter (overrides --filters/--filter/--sort; pagination stays on the query string)
- name: datastore-import-failures
  description: Get a CSV import job's failures (GET /api/DataStore/{id}/import-failures/{jobId}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: job-id
    type: string
    required: true
    description: CSV import jobId
- name: datastore-import-start
  description: Start a CSV import job (POST /api/DataStore/{id}/import-start) from a --file.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: file
    type: string
    required: true
    description: local CSV file to import
- name: datastore-list
  description: List data stores (GET /api/DataStore).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: search
    type: string
    required: false
    description: filter by name (searchName), if supported
- name: datastore-list-restricted
  description: List data stores the caller is restricted to (GET /api/DataStore/restricted).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: datastore-modify-column
  description: Modify one column (PATCH /api/DataStore/{id}/column) from --payload.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: payload
    type: string
    required: true
    description: 'DataStoreModifyColumnDto as JSON: {"originalColumn":{...},"updatedColumn":{...}}'
- name: datastore-update
  description: Update a data store's metadata (PUT /api/DataStore) from --payload (include id).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: 'DataStoreMetadataDto as JSON: {"name","description"?,"columns"?:[{"name","dataTypeId","isList"?,"isPrimaryKey"?,"isRequired"?}]}'
- name: datastore-update-row
  description: Update rows (PUT /api/DataStore/{id}/rows) — set --values on rows a --filter/--filters selects (filter mandatory).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data store id (dataStoreId)
  - name: values
    type: string
    required: false
    description: columns to set, as a JSON object {"Status":"Done"} (keyed by display name)
  - name: filters
    type: string
    required: false
    description: 'simple conditions as JSON: [{"column","operator"(name or number),"value","auxValue"?}] combined by --logic. operator names: equals/contains/startsWith/greaterThan/between/in/isNull/... (QueryOperators)'
  - name: filter
    type: string
    required: false
    description: 'raw filter GROUP as JSON (a full recursive DataStoreQueryFilterGroupDto: {"id","logic","items":[...]}) sent verbatim; overrides --filters'
  - name: logic
    type: string
    required: false
    description: 'how --filters combine: and | or (default and)'
  - name: body
    type: string
    required: false
    description: raw DataStoreUpdateRowRequestDto JSON sent verbatim (overrides --values/--filter)
- name: datatype-add-attribute
  description: Add one attribute to a model — compiles it; a model-typed attr inlines the child + keeps it reusable.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: parent model id
  - name: name
    type: string
    required: true
    description: attribute name
  - name: data-type
    type: string
    required: true
    description: primitive name (string/integer/…), an existing model name, or a guid
  - name: is-list
    type: boolean
    required: false
    description: attribute holds a list of the type
  - name: display-name
    type: string
    required: false
    description: ''
  - name: json-property
    type: string
    required: false
    description: JSON key (defaults to name)
  - name: hidden
    type: boolean
    required: false
    description: ''
  - name: public
    type: boolean
    required: false
    description: mark the attribute isPublic
  - name: force
    type: boolean
    required: false
    description: suppress the store-backing caution (B-048 cluster 4a)
- name: datatype-change-to-public
  description: Promote a private inner model (from fromJson) to public so it's reusable (POST /api/DataTypes/changeToPublic).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: root-id
    type: string
    required: true
    description: root/top model id
  - name: id
    type: string
    required: true
    description: inner model id to promote/clone
- name: datatype-clone
  description: Clone an inner data model (POST /api/DataTypes/clone).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: root-id
    type: string
    required: true
    description: root/top model id
  - name: id
    type: string
    required: true
    description: inner model id to promote/clone
- name: datatype-create
  description: Create a PROCESIO datatype from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: datatype-delete
  description: Delete a data model (DELETE /api/DataTypes/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data model id
- name: datatype-delete-attribute
  description: Delete one attribute (DELETE /api/dataTypes/attribute/{id}/{attrId}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: parent model id
  - name: attribute
    type: string
    required: true
    description: attribute id or current name
- name: datatype-edit
  description: Edit a PROCESIO datatype to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: datatype-edit-attribute
  description: Edit one attribute (PUT /api/dataTypes/attribute/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: parent model id
  - name: attribute
    type: string
    required: true
    description: attribute id or current name
  - name: name
    type: string
    required: false
    description: ''
  - name: display-name
    type: string
    required: false
    description: ''
  - name: data-type
    type: string
    required: false
    description: new type (primitive/model/guid)
  - name: json-property
    type: string
    required: false
    description: ''
  - name: is-list
    type: string
    required: false
    description: true/false (omit to leave unchanged)
  - name: hidden
    type: string
    required: false
    description: true/false
  - name: public
    type: string
    required: false
    description: isPublic true/false
- name: datatype-get
  description: Get a data model with its attributes (GET /api/DataTypes/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: data model id
- name: delete-actions-by-id
  description: 'DELETE /api/Actions/{id}  [Actions] - Permission required: CustomActions.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-actions-folders-by-id
  description: 'DELETE /api/Actions/folders/{id}  [Actions] - Permission required: ProcessDesigner.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-actions-templates-by-id
  description: 'DELETE /api/Actions/templates/{id}  [Actions] - Permission required: ProcessDesigner.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-apikey
  description: 'DELETE /api/ApiKey  [ApiKey] - Permission required: ApiKey.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-apikey-by-id
  description: 'DELETE /api/ApiKey/{id}  [ApiKey] - Permission required: ApiKey.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-authentication-logout
  description: 'DELETE /api/Authentication/logOut  [Authentication] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-by-masterworkspaceurl-by-workspaceurl-by-entityurl
  description: DELETE /{masterWorkspaceUrl}/{workspaceUrl}/{entityUrl}  [CustomUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: masterWorkspaceUrl
    type: string
    required: true
    description: path segment {masterWorkspaceUrl}
  - name: workspaceUrl
    type: string
    required: true
    description: path segment {workspaceUrl}
  - name: entityUrl
    type: string
    required: true
    description: path segment {entityUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-by-tinyurl
  description: DELETE /{tinyUrl}  [TinyUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: tinyUrl
    type: string
    required: true
    description: path segment {tinyUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-credentials-by-id
  description: 'DELETE /api/Credentials/{id}  [Credentials] - Permission required: Credentials.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-customurl-formtemplate-by-id
  description: 'DELETE /api/CustomUrl/FormTemplate/{id}  [CustomUrlForm] - Permission required: FormTemplate.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-customurl-masterworkspace-by-id
  description: 'DELETE /api/CustomUrl/MasterWorkspace/{id}  [CustomUrlMaster] - Permission required: MasterWorkspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-customurl-webhook-by-id
  description: 'DELETE /api/CustomUrl/Webhook/{id}  [CustomUrlWebhook] - Permission required: Webhook.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-customurl-workspace-by-id
  description: 'DELETE /api/CustomUrl/Workspace/{id}  [CustomUrlWorkspace] - Permission required: Workspace.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-datatypes-attribute-by-rootdatatypeid-by-attributeid
  description: 'DELETE /api/DataTypes/attribute/{rootDataTypeId}/{attributeId}  [DataTypes] - Permission required: DataModels.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: rootDataTypeId
    type: string
    required: true
    description: path segment {rootDataTypeId}
  - name: attributeId
    type: string
    required: true
    description: path segment {attributeId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-datatypes-by-id
  description: 'DELETE /api/DataTypes/{id}  [DataTypes] - Permission required: DataModels.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-documenttemplate-by-id
  description: 'DELETE /api/DocumentTemplate/{id}  [DocumentTemplate] - Permission required: DocumentDesigner.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-form
  description: 'DELETE /api/Form  [Form] - Permission required: FormInstance.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-form-by-id
  description: 'DELETE /api/Form/{id}  [Form] - Permission required: FormInstance.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-formapplication-by-id
  description: 'DELETE /api/FormApplication/{id}  [FormApplication] - Permission required: FormTemplate.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-formtemplate-by-id
  description: 'DELETE /api/FormTemplate/{id}  [FormTemplate] - Permission required: FormTemplate.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-projects-by-id
  description: 'DELETE /api/Projects/{id}  [ProcessTemplate] - Permission required: ProcessDesigner.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-projects-instances-by-id-dataretention
  description: 'DELETE /api/Projects/instances/{id}/dataRetention  [ProcessInstance] - Permission required: ProcessInstance.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: flowTemplateId
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-schedule
  description: Delete a schedule (DELETE /api/Schedules/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: schedule id (scheduleId)
- name: delete-schedules-by-scheduleid
  description: 'DELETE /api/Schedules/{scheduleId}  [Schedules] - Permission required: Schedule.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: scheduleId
    type: string
    required: true
    description: path segment {scheduleId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-userpreferences
  description: 'DELETE /api/UserPreferences  [UserPreferences] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-webhooks-by-webhookid
  description: 'DELETE /api/Webhooks/{webhookId}  [Webhooks] - Permission required: Webhook.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: webhookId
    type: string
    required: true
    description: path segment {webhookId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-webhooks-by-webhookid-undo
  description: 'DELETE /api/Webhooks/{webhookId}/undo  [Webhooks] - Permission required: Webhook.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: webhookId
    type: string
    required: true
    description: path segment {webhookId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-webhooks-launch-by-id
  description: 'DELETE /api/Webhooks/launch/{id}  [WebhookLaunch] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-workspace-by-workspacetodelete
  description: 'DELETE /api/Workspace/{workspaceToDelete}  [Workspace] - Permission required: MasterWorkspace.Delete'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: workspaceToDelete
    type: string
    required: true
    description: path segment {workspaceToDelete}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: delete-workspace-invite-by-id
  description: 'DELETE /api/Workspace/invite/{id}  [Workspace] - Permission required: Workspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: targetWorkspace
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: document-create
  description: Create a PROCESIO document from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: document-delete
  description: Delete a resource by id (DELETE /api/DocumentTemplate/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: document template id
- name: document-edit
  description: Edit a PROCESIO document to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: document-get
  description: Get one resource by id (GET /api/DocumentTemplate/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: document template id
- name: document-list
  description: List documents (GET /api/DocumentTemplate).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: duplicate-process
  description: 'Duplicate a process (POST /api/Projects/{id}/duplicate) and return the copy''s id + designer URL (found by diffing the workspace''s project list). Workspace-scoped: pass --workspace-id.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id to duplicate
- name: export
  description: Export components to a .procesio file (Transport). Accepts names/ids/all per type; credentials excluded by default; --dry-run to preview the selection.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: data-models
    type: string
    required: false
    description: data-model names/ids/all (comma-separated)
  - name: processes
    type: string
    required: false
    description: process names (title)/ids/all (comma-separated)
  - name: documents
    type: string
    required: false
    description: document names/ids/all (comma-separated)
  - name: webhooks
    type: string
    required: false
    description: webhook names/ids/all (comma-separated)
  - name: forms
    type: string
    required: false
    description: form names/ids/all (comma-separated)
  - name: credentials
    type: string
    required: false
    description: 'credential names/ids/all (default: none — excluded)'
  - name: data-stores
    type: string
    required: false
    description: data-store names/ids/all (comma-separated). A process that reads or writes a store does NOT carry the store itself — name it here or the pack arrives with a dangling reference
  - name: export-sensitive-data
    type: boolean
    required: false
    description: include credential secrets (default off)
  - name: output
    type: string
    required: false
    description: output .procesio path (default procesio-export.procesio)
  - name: dry-run
    type: boolean
    required: false
    description: resolve names + build the selection, but do not export or save
- name: file-download
  description: Download a flow-instance file (Generate-Document output etc.) via the header-based GET /api/File/download.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: from-run
    type: string
    required: false
    description: path to a run-process result JSON; derive the file + ids from it
  - name: var
    type: string
    required: false
    description: 'with --from-run: the output variable name to download (default: the first file variable)'
  - name: file-path
    type: string
    required: false
    description: the file DTO 'path' (flow/flow-<id>/flow-instance-<id>/variable-<id>/<fileId>)
  - name: variable-id
    type: string
    required: false
    description: output variable id
  - name: instance-id
    type: string
    required: false
    description: flow instance id
  - name: flow-template-id
    type: string
    required: false
    description: process (flow template) id
  - name: out
    type: string
    required: false
    description: 'output file path or directory (default: the filename in the cwd)'
  - name: is-archived
    type: boolean
    required: false
    description: fetch from archived storage
- name: flow-lint
  description: 'Designer-layer ''Process Errors'' lint on a live flow (the check that blocks designer SAVE, which POST /Projects/validate does NOT catch): stale subprocess side-pannel ids, unmapped/dead required subprocess inputs, subprocess outputs mapped to non-output (non-type-30) vars, Execute Query with null Output, and Node code binding error-scope/undeclared vars. clean=true when none.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: flow (process) id to lint
- name: form-add-element
  description: Add one or more controls to a LIVE form (authoring-config elements), splicing them into Data.elements AND the data model without regenerating what is already there - so every existing field path, and every process map that references one, survives. --parent places them in a container by name or id; --dry-run previews.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: elements
    type: string
    required: false
    description: one authoring-config element object, or a JSON list of them
  - name: elements-file
    type: string
    required: false
    description: path to a JSON file with the element(s)
  - name: parent
    type: string
    required: false
    description: place them inside this container, by element name or id
  - name: dry-run
    type: boolean
    required: false
    description: build the new Data and report what would be added, without saving
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: form-create
  description: Create a PROCESIO form from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: form-delete
  description: Delete a resource by id (DELETE /api/FormTemplate/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
- name: form-duplicate
  description: POST /api/FormTemplate/{id}/duplicate.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
- name: form-edit
  description: Edit a PROCESIO form to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: form-get
  description: Get one resource by id (GET /api/FormTemplate/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
- name: form-get-code
  description: Read a form's global CSS + JavaScript (decrypts Data.code).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: css-out
    type: string
    required: false
    description: also write the CSS to this file
  - name: js-out
    type: string
    required: false
    description: also write the JavaScript to this file
- name: form-get-element
  description: Read one element's configs from a live form (id or name).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: element
    type: string
    required: true
    description: element id, or its `name` config as the designer shows it
  - name: key
    type: string
    required: false
    description: 'only return this config key (repeatable; default: all)'
- name: form-get-element-events
  description: List one element's event handlers, by trigger.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: element
    type: string
    required: true
    description: element id, or its designer 'name' (e.g. doccipf)
- name: form-list
  description: List forms (GET /api/FormTemplate/all/basic).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: form-set-code
  description: 'Set a form''s global CSS + JavaScript in place (surgical: only Data.code changes; omitted side is preserved; returns the previous code).'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: css
    type: string
    required: false
    description: CSS source as an inline string
  - name: css-file
    type: string
    required: false
    description: path to a .css file
  - name: javascript
    type: string
    required: false
    description: JavaScript source as an inline string
  - name: js-file
    type: string
    required: false
    description: path to a .js file
  - name: dry-run
    type: boolean
    required: false
    description: report the change (and the previous code) without writing
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: form-set-element-config
  description: 'Set one element''s plain configs in place (surgical: only that element''s config values change, ids preserved; returns the previous values). Event configs go through form-set-element-event.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: element
    type: string
    required: true
    description: element id, or its `name` config as the designer shows it
  - name: set
    type: string
    required: false
    description: key=value to write (repeatable); the value is parsed as JSON when it parses, otherwise stored as a plain string. A value that LOOKS like JSON (true/42/[..]) is stored as that type — for a literal string that looks like JSON, use --set-file (always a string)
  - name: set-file
    type: string
    required: false
    description: key=path to write a config's value from a file (repeatable) — for long HTML labels
  - name: dry-run
    type: boolean
    required: false
    description: report the change (and the previous values) without writing
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: form-set-element-event
  description: 'Wire one element''s trigger to RUN_PROCESS / RUN_JAVASCRIPT / RUN_DATA_STORE_OPERATION in place (surgical: only that element''s event config changes; process-variable names in inputMap/outputMap are resolved to guids; a DataStore op takes --data-store-id + --operation).'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: element
    type: string
    required: true
    description: element id, or its designer 'name' (e.g. doccipf)
  - name: 'on'
    type: string
    required: true
    description: 'trigger: click | input | stepChange | paginationChanged | rowAdded'
  - name: action
    type: string
    required: true
    description: RUN_PROCESS | RUN_JAVASCRIPT | RUN_DATA_STORE_OPERATION
  - name: config
    type: string
    required: false
    description: event config as JSON
  - name: data-store-id
    type: string
    required: false
    description: 'RUN_DATA_STORE_OPERATION: the target data store id'
  - name: operation
    type: string
    required: false
    description: 'RUN_DATA_STORE_OPERATION: READ | ADD | UPDATE | DELETE'
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: type
    type: string
    required: false
    description: override the event type (default follows the trigger)
  - name: replace
    type: boolean
    required: false
    description: replace ALL of this trigger's events instead of appending (destructive; warns when it discards more than one)
  - name: replace-action
    type: string
    required: false
    description: replace only this trigger's events whose action is this (RUN_PROCESS/RUN_JAVASCRIPT), preserving the others and order
  - name: dry-run
    type: boolean
    required: false
    description: build the event and report it without writing
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: form-update
  description: 'Safely save an arbitrary change to a LIVE form: GET it, deep-merge a --data patch into its Data (and/or override --name/--status/--state/--is-private), then PUT the server-accepted PascalCase DTO. Preserves elements/theme/code (never rebuilds like form-edit). Last-write-wins guard (--force); --dry-run previews the DTO.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: form template id
  - name: data
    type: string
    required: false
    description: JSON object deep-merged into the form's Data (a merge patch)
  - name: data-file
    type: string
    required: false
    description: path to a JSON file with the Data merge patch
  - name: name
    type: string
    required: false
    description: rename the form (top-level Name)
  - name: status
    type: integer
    required: false
    description: 'publication status: 1=published, 0=draft'
  - name: state
    type: string
    required: false
    description: enabled flag (true/false)
  - name: is-private
    type: string
    required: false
    description: 'reachability: false + --status 1 makes the form answer an anonymous visitor on its custom URL; true keeps it behind a PROCESIO login'
  - name: dry-run
    type: boolean
    required: false
    description: build the PUT DTO and return it without saving (GET only)
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: get-actions
  description: 'GET /api/Actions  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: actionFilter
    type: string
    required: false
    description: query parameter
  - name: getFullAction
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-by-id
  description: 'GET /api/Actions/{id}  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-category-by-category
  description: 'GET /api/Actions/category/{category}  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: category
    type: string
    required: true
    description: path segment {category}
  - name: getFullAction
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-event-by-id
  description: 'GET /api/Actions/event/{id}  [Actions] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-folders
  description: 'GET /api/Actions/folders  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-folders-by-id
  description: 'GET /api/Actions/folders/{id}  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-node
  description: 'GET /api/Actions/node  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: getFullAction
    type: string
    required: false
    description: query parameter
  - name: isCustom
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-restricted
  description: 'GET /api/Actions/restricted  [Actions] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: actionFilter
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-templates-by-id
  description: 'GET /api/Actions/templates/{id}  [Actions] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-actions-test-by-id
  description: 'GET /api/Actions/test/{id}  [Actions] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-analytics-executionenvironment-concurrency
  description: 'GET /api/analytics/executionEnvironment/concurrency  [Resources] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: statsType
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-analytics-executionenvironment-topprocesses
  description: 'GET /api/analytics/executionEnvironment/topProcesses  [Resources] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: topCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-apikey
  description: 'GET /api/ApiKey  [ApiKey] - Permission required: ApiKey.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-by-masterworkspaceurl-by-workspaceurl-by-entityurl
  description: GET /{masterWorkspaceUrl}/{workspaceUrl}/{entityUrl}  [CustomUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: masterWorkspaceUrl
    type: string
    required: true
    description: path segment {masterWorkspaceUrl}
  - name: workspaceUrl
    type: string
    required: true
    description: path segment {workspaceUrl}
  - name: entityUrl
    type: string
    required: true
    description: path segment {entityUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-by-tinyurl
  description: GET /{tinyUrl}  [TinyUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: tinyUrl
    type: string
    required: true
    description: path segment {tinyUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials
  description: 'GET /api/Credentials  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-authorize-by-id
  description: 'GET /api/Credentials/authorize/{id}  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-by-id
  description: 'GET /api/Credentials/{id}  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-by-id-restricted
  description: 'GET /api/Credentials/{id}/restricted  [Credentials] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-count
  description: 'GET /api/Credentials/count  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-list-by-typeid
  description: 'GET /api/Credentials/list/{typeId}  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: typeId
    type: string
    required: true
    description: path segment {typeId}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-list-by-typeid-count
  description: 'GET /api/Credentials/list/{typeId}/count  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: typeId
    type: string
    required: true
    description: path segment {typeId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-list-by-typeid-restricted
  description: 'GET /api/Credentials/list/{typeId}/restricted  [Credentials] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: typeId
    type: string
    required: true
    description: path segment {typeId}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-types
  description: 'GET /api/Credentials/types  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-types-by-id
  description: 'GET /api/Credentials/types/{id}  [Credentials] - Permission required: Credentials.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-credentials-verbs-by-templateid
  description: 'GET /api/Credentials/verbs/{templateId}  [Credentials] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: templateId
    type: string
    required: true
    description: path segment {templateId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-customurl-formtemplate-by-id
  description: 'GET /api/CustomUrl/FormTemplate/{id}  [CustomUrlForm] - Permission required: FormTemplate.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-customurl-masterworkspace
  description: 'GET /api/CustomUrl/MasterWorkspace  [CustomUrlMaster] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-customurl-webhook-by-id
  description: 'GET /api/CustomUrl/Webhook/{id}  [CustomUrlWebhook] - Permission required: Webhook.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-customurl-workspace
  description: 'GET /api/CustomUrl/Workspace  [CustomUrlWorkspace] - Permission required: Workspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-customurl-workspace-master
  description: 'GET /api/CustomUrl/Workspace/master  [CustomUrlWorkspace] - Permission required: Workspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes
  description: 'GET /api/DataTypes  [DataTypes] - Permission required: DataModels.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: addProperties
    type: string
    required: false
    description: query parameter
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: includeProcesioEntries
    type: string
    required: false
    description: query parameter
  - name: includeExternalEntries
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-by-id-by-type
  description: 'GET /api/DataTypes/{id}/{type}  [DataTypes] - Permission required: DataModels.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: type
    type: string
    required: true
    description: path segment {type}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-by-value
  description: 'GET /api/DataTypes/{value}  [DataTypes] - Permission required: DataModels.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: value
    type: string
    required: true
    description: path segment {value}
  - name: displayName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-count
  description: 'GET /api/DataTypes/count  [DataTypes] - Permission required: DataModels.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: includeProcesioEntries
    type: string
    required: false
    description: query parameter
  - name: includeExternalEntries
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-primary
  description: 'GET /api/DataTypes/primary  [DataTypes] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-primary-count
  description: 'GET /api/DataTypes/primary/count  [DataTypes] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-procesio
  description: 'GET /api/DataTypes/procesio  [DataTypes] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-procesio-count
  description: 'GET /api/DataTypes/procesio/count  [DataTypes] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-datatypes-restricted
  description: 'GET /api/DataTypes/restricted  [DataTypes] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-documenttemplate
  description: 'GET /api/DocumentTemplate  [DocumentTemplate] - Permission required: DocumentDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-documenttemplate-by-id
  description: 'GET /api/DocumentTemplate/{id}  [DocumentTemplate] - Permission required: DocumentDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-documenttemplate-by-id-restricted
  description: 'GET /api/DocumentTemplate/{id}/restricted  [DocumentTemplate] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-documenttemplate-restricted
  description: 'GET /api/DocumentTemplate/restricted  [DocumentTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-file-download
  description: 'GET /api/File/download  [File] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: isArchived
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-file-download-action-event
  description: 'GET /api/File/download/action-event  [File] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-file-download-schedule
  description: 'GET /api/File/download/schedule  [File] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-file-download-testaction
  description: 'GET /api/File/download/testAction  [File] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-assigned
  description: 'GET /api/Form/assigned  [Form] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: awaitingAction
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-by-id
  description: 'GET /api/Form/{id}  [Form] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-by-pid-all
  description: 'GET /api/Form/{pid}/all  [Form] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pid
    type: string
    required: true
    description: path segment {pid}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-by-pid-count
  description: 'GET /api/Form/{pid}/count  [Form] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pid
    type: string
    required: true
    description: path segment {pid}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-chain-by-chainid
  description: 'GET /api/Form/chain/{chainId}  [FormChain] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: chainId
    type: string
    required: true
    description: path segment {chainId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-chain-by-formtemplateid-all
  description: 'GET /api/Form/chain/{formTemplateId}/all  [FormChain] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-form-download
  description: 'GET /api/Form/download  [FileForm] - Permission required: FormInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formapplication-all
  description: 'GET /api/FormApplication/all  [FormApplication] - Permission required: FormTemplate.None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formapplication-all-by-pid
  description: 'GET /api/FormApplication/all/{pid}  [FormApplication] - Permission required: FormTemplate.None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pid
    type: string
    required: true
    description: path segment {pid}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formapplication-all-filter
  description: 'GET /api/FormApplication/all/filter  [FormApplication] - Permission required: FormTemplate.None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: state
    type: string
    required: false
    description: query parameter
  - name: type
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formapplication-by-id
  description: 'GET /api/FormApplication/{id}  [FormApplication] - Permission required: FormTemplate.None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formprocess-by-formtemplateid-by-processtemplateid
  description: 'GET /api/FormProcess/{formTemplateId}/{processTemplateId}  [FormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: processTemplateId
    type: string
    required: true
    description: path segment {processTemplateId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formprocess-by-formtemplateid-by-processtemplateid-by-processinstanceid-variables
  description: 'GET /api/FormProcess/{formTemplateId}/{processTemplateId}/{processInstanceId}/variables  [FormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: processTemplateId
    type: string
    required: true
    description: path segment {processTemplateId}
  - name: processInstanceId
    type: string
    required: true
    description: path segment {processInstanceId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formprocess-download
  description: 'GET /api/FormProcess/download  [FileFormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate
  description: 'GET /api/FormTemplate  [FormTemplate] - Permission required: FormTemplate.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate-all-basic
  description: 'GET /api/FormTemplate/all/basic  [FormTemplate] - Permission required: FormTemplate.None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate-by-id
  description: 'GET /api/FormTemplate/{id}  [FormTemplate] - Permission required: FormTemplate.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate-by-workspaceid-by-id
  description: 'GET /api/FormTemplate/{workspaceId}/{id}  [FormTemplate] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: workspaceId
    type: string
    required: true
    description: path segment {workspaceId}
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate-processtemplate-by-id
  description: 'GET /api/FormTemplate/processTemplate/{id}  [FormTemplate] - Permission required: FormTemplate.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-formtemplate-processtemplate-list
  description: 'GET /api/FormTemplate/processTemplate/list  [FormTemplate] - Permission required: FormTemplate.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-instance-output
  description: Get an instance's output (GET /api/Projects/instances/{id}/output).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: instance id
  - name: flow-template-id
    type: string
    required: false
    description: flowTemplateId (the process id)
- name: get-instance-status
  description: Get an instance's status/variables (GET /api/Projects/instances/{id}/status).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: instance id
  - name: flow-template-id
    type: string
    required: false
    description: flowTemplateId (the process id)
  - name: variables
    type: boolean
    required: false
    description: include variables (getVariables=true)
  - name: actions
    type: boolean
    required: false
    description: include actions (getActions=true)
- name: get-notifications
  description: 'GET /api/Notifications  [Notifications] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-process
  description: Get one process by id (GET /api/Projects/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
- name: get-process-payload
  description: Get a process's input-variable payload shape for run-process.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
- name: get-projects
  description: 'GET /api/Projects  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id
  description: 'GET /api/Projects/{id}  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-history
  description: 'GET /api/Projects/{id}/history  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: filter
    type: string
    required: false
    description: query parameter
  - name: monthFilter
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-instances
  description: 'GET /api/Projects/{id}/instances  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: filterStatus
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-instances-by-instanceid-customresponse
  description: 'GET /api/Projects/{id}/instances/{instanceId}/customResponse  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: instanceId
    type: string
    required: true
    description: path segment {instanceId}
  - name: isArchived
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-instances-count
  description: 'GET /api/Projects/{id}/instances/count  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-payload
  description: 'GET /api/Projects/{id}/payload  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-restricted
  description: 'GET /api/Projects/{id}/restricted  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-restricted-schedules
  description: 'GET /api/Projects/{id}/restricted/schedules  [ProcessTemplate] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-by-id-used
  description: 'GET /api/Projects/{id}/used  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-count
  description: 'GET /api/Projects/count  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-instances-by-id-output
  description: 'GET /api/Projects/instances/{id}/output  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: flowTemplateId
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-instances-by-id-status
  description: 'GET /api/Projects/instances/{id}/status  [ProcessInstance] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: flowTemplateId
    type: string
    required: false
    description: query parameter
  - name: isArchived
    type: string
    required: false
    description: query parameter
  - name: getActions
    type: string
    required: false
    description: query parameter
  - name: getVariables
    type: string
    required: false
    description: query parameter
  - name: getInternalVariables
    type: string
    required: false
    description: query parameter
  - name: addExtras
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-notifications-by-flowid
  description: 'GET /api/Projects/notifications/{flowId}  [ProcessTemplate] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: flowId
    type: string
    required: true
    description: path segment {flowId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-projects-restricted-schedules
  description: 'GET /api/Projects/restricted/schedules  [ProcessTemplate] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resources-analytics-instances-by-id-details
  description: 'GET /api/Resources/analytics/instances/{id}/details  [Resources] - Permission required: ProcessInstance.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resources-analytics-processes
  description: 'GET /api/Resources/analytics/processes  [Resources] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resources-analytics-processes-by-id-details
  description: 'GET /api/Resources/analytics/processes/{id}/details  [Resources] - Permission required: ProcessDesigner.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resources-used
  description: 'GET /api/Resources/used  [Resources] - Permission required: Workspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resources-used-subworkspaces
  description: 'GET /api/Resources/used/subWorkspaces  [Resources] - Permission required: Workspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: startDate
    type: string
    required: false
    description: query parameter
  - name: endDate
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-resourcetrackingconfig
  description: 'GET /api/ResourceTrackingConfig  [ResourceTrackingConfig] - Permission required: Workspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-schedule
  description: Get one schedule by id; use --redact-process-inputs for safe evidence.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: schedule id (scheduleId)
  - name: redact-process-inputs
    type: boolean
    required: false
    description: replace every returned processInputs[].value with [REDACTED]
- name: get-schedule-notifications
  description: Get a schedule's notifications (GET /api/Schedules/notifications/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: schedule id (scheduleId)
- name: get-schedules
  description: 'GET /api/Schedules  [Schedules] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-schedules-by-scheduleid
  description: 'GET /api/Schedules/{scheduleId}  [Schedules] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: scheduleId
    type: string
    required: true
    description: path segment {scheduleId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-schedules-notifications-by-scheduleid
  description: 'GET /api/Schedules/notifications/{scheduleId}  [Schedules] - Permission required: Schedule.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: scheduleId
    type: string
    required: true
    description: path segment {scheduleId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-subscriptions
  description: 'GET /api/Subscriptions  [Subscriptions] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-subscriptions-by-subscriptionid
  description: 'GET /api/Subscriptions/{subscriptionId}  [Subscriptions] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: subscriptionId
    type: string
    required: true
    description: path segment {subscriptionId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-transport-export
  description: 'GET /api/Transport/export  [Transport] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions
  description: 'GET /api/UserPermissions  [UserPermissions] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions-by-userid
  description: 'GET /api/UserPermissions/{userId}  [UserPermissions] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: userId
    type: string
    required: true
    description: path segment {userId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions-entities
  description: 'GET /api/UserPermissions/entities  [UserPermissions] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions-roles
  description: 'GET /api/UserPermissions/roles  [UserPermissions] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions-usertypes
  description: 'GET /api/UserPermissions/userTypes  [UserPermissions] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpermissions-workspace-by-workspaceid-default
  description: 'GET /api/UserPermissions/workspace/{workspaceId}/default  [UserPermissions] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: workspaceId
    type: string
    required: true
    description: path segment {workspaceId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-userpreferences
  description: 'GET /api/UserPreferences  [UserPreferences] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-users-me
  description: 'GET /api/Users/me  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-webhooks
  description: 'GET /api/Webhooks  [Webhooks] - Permission required: Webhook.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: searchName
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-webhooks-by-webhookid
  description: 'GET /api/Webhooks/{webhookId}  [Webhooks] - Permission required: Webhook.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: webhookId
    type: string
    required: true
    description: path segment {webhookId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-webhooks-by-webhookid-used
  description: 'GET /api/Webhooks/{webhookId}/used  [Webhooks] - Permission required: Webhook.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: webhookId
    type: string
    required: true
    description: path segment {webhookId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-webhooks-datamodels-by-webhookid
  description: 'GET /api/Webhooks/datamodels/{webhookId}  [Webhooks] - Permission required: Webhook.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: webhookId
    type: string
    required: true
    description: path segment {webhookId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-webhooks-launch-by-id
  description: 'GET /api/Webhooks/launch/{id}  [WebhookLaunch] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspace-by-parentid-otp
  description: 'GET /api/Workspace/{parentId}/otp  [Workspace] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: parentId
    type: string
    required: true
    description: path segment {parentId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspace-by-parentid-subworkspaces
  description: 'GET /api/Workspace/{parentId}/subworkspaces  [Workspace] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: parentId
    type: string
    required: true
    description: path segment {parentId}
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspace-by-parentid-subworkspaces-by-id
  description: 'GET /api/Workspace/{parentId}/subworkspaces/{id}  [Workspace] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: parentId
    type: string
    required: true
    description: path segment {parentId}
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspace-settings-default
  description: 'GET /api/Workspace/settings/default  [Workspace] - Permission required: MasterWorkspace.Read'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspace-users
  description: 'GET /api/Workspace/users  [Workspace] - Permission required: Workspace.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: pageNumber
    type: string
    required: false
    description: query parameter
  - name: pageItemCount
    type: string
    required: false
    description: query parameter
  - name: targetWorkspace
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: get-workspaces
  description: 'GET /api/Workspaces  [Workspace] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: import
  description: Import components from a .procesio bundle (POST /api/Transport/import); mirrors export.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: file
    type: string
    required: true
    description: path to the .procesio bundle to import
  - name: no-override-data
    type: boolean
    required: false
    description: send overrideData=false (default true)
  - name: no-data-types
    type: boolean
    required: false
    description: send importDataTypes=false (default true)
  - name: no-flows
    type: boolean
    required: false
    description: send importFlows=false (default true)
  - name: no-credentials
    type: boolean
    required: false
    description: send importCredentials=false (default true)
  - name: no-documents
    type: boolean
    required: false
    description: send importDocuments=false (default true)
  - name: no-forms
    type: boolean
    required: false
    description: send importForms=false (default true)
  - name: no-data-stores
    type: boolean
    required: false
    description: send importDataStores=false (default true)
- name: inspect-flow
  description: 'Structural summary of a flow (offline): counts, action families, branches, subprocess calls, resources, variables, advisory smells.'
  args:
  - name: in
    type: string
    required: true
    description: '.procesio export or flow JSON: a path, ''-'' for stdin, or raw JSON'
  - name: flow-id
    type: string
    required: false
    description: which flow when the bundle has more than one
- name: layout-flow
  description: Recompute tidy canvas positions for a flow (deterministic, offline). Writes positions back into a re-importable bundle (--out) or prints it.
  args:
  - name: in
    type: string
    required: true
    description: '.procesio export or flow JSON: a path, ''-'' for stdin, or raw JSON'
  - name: flow-id
    type: string
    required: false
    description: which flow when the bundle has more than one
  - name: out
    type: string
    required: false
    description: write the re-laid-out bundle here (omit to print it)
  - name: only
    type: string
    required: false
    description: comma-separated action ids to reposition WITHOUT reshuffling the rest (re-tidy just these; everything else stays byte-identical)
  - name: cluster
    type: boolean
    required: false
    description: 'cluster consecutive horizontal actions: fold a long run (>=5 1-in/1-out actions, outside a For-Each) into a compact serpentine grid to conserve space. Opt-in.'
  - name: min-crossings
    type: boolean
    required: false
    description: 'best-effort edge-crossing reduction: nudge movable actions to cut line crossings (minimizes, not guaranteed zero). Opt-in.'
  - name: dry-run
    type: boolean
    required: false
    description: emit the computed positions only; write/print nothing else
- name: layout-report
  description: 'Engine-agnostic layout verification: readability report for a laid-out flow (hard issues + crossings, edge lengths, vertical rows, For-Each padding/size). Works for ANY layout engine. Offline via --in, or LIVE via --id + --workspace-id.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: in
    type: string
    required: false
    description: '.procesio export or flow JSON: a path, ''-'' for stdin, or raw JSON (offline). Omit and pass --id to verify a LIVE process instead.'
  - name: id
    type: string
    required: false
    description: verify a LIVE process by id (needs --workspace-id); alternative to --in
  - name: flow-id
    type: string
    required: false
    description: which flow when the bundle has more than one
- name: layout-resource-map
  description: Position the cross-process (process→process) call graph from a bundle (offline).
  args:
  - name: in
    type: string
    required: true
    description: '.procesio export bundle: a path, ''-'' for stdin, or raw JSON'
  - name: root-flow-id
    type: string
    required: false
    description: the LVL0 process to center the map on (optional)
  - name: levels
    type: integer
    required: false
    description: 'max depth each side (default: unlimited)'
- name: list-actions-catalog
  description: List the action/connector catalog (GET /api/Actions).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: filter
    type: string
    required: false
    description: actionFilter value
  - name: full
    type: boolean
    required: false
    description: getFullAction=true
  - name: by-family
    type: boolean
    required: false
    description: group the catalog by family (control/scripting/data/integration/…)
  - name: family
    type: string
    required: false
    description: return only one family's actions
- name: list-api-keys
  description: List API keys in the workspace (GET /api/ApiKey).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: list-connection-types
  description: List supported connection types (GET /api/Credentials/types).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: list-connections
  description: List stored connection credentials (GET /api/Credentials).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: search
    type: string
    required: false
    description: searchName filter
- name: list-credentials
  description: List stored credential profiles (names/types/workspaces only - no secrets).
  args: []
- name: list-datatypes
  description: List data types (GET /api/DataTypes).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: search
    type: string
    required: false
    description: searchName filter
- name: list-endpoints
  description: Browse the bundled Swagger index of all PROCESIO endpoints.
  args:
  - name: filter
    type: string
    required: false
    description: substring match on path/tag/summary
  - name: tag
    type: string
    required: false
    description: exact tag filter, e.g. ProcessTemplate
  - name: method
    type: string
    required: false
    description: filter by HTTP method
  - name: tags-only
    type: boolean
    required: false
    description: just list the available tags + counts
- name: list-environments
  description: List known PROCESIO environments (URLs, default, bound credentials).
  args: []
- name: list-instances
  description: List a process's run instances (GET /api/Projects/{id}/instances).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: status
    type: string
    required: false
    description: filterStatus value
- name: list-processes
  description: List processes (GET /api/Projects).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: search
    type: string
    required: false
    description: filter by process name (searchName)
- name: list-project-schedules
  description: List a project's schedules (GET /api/Projects/{id}/restricted/schedules).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: project (process) id
- name: list-schedules
  description: List schedules (GET /api/Schedules).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: search
    type: string
    required: false
    description: filter by schedule name (searchName)
- name: list-subworkspaces
  description: List a master's sub-workspaces, active-only by default (--include-removed for soft-deleted).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: parent-id
    type: string
    required: true
    description: master workspace GUID whose sub-workspaces to list
  - name: include-removed
    type: boolean
    required: false
    description: include soft-deleted (workspaceState='removed') workspaces
- name: list-workspace-users
  description: List users in a workspace (GET /api/Workspace/users).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: page
    type: integer
    required: false
    description: page number (1-based)
  - name: page-size
    type: integer
    required: false
    description: items per page
  - name: target-workspace
    type: string
    required: false
    description: targetWorkspace id (sub-workspace)
- name: list-workspaces
  description: List the caller's workspaces (GET /api/Workspaces, active-only).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: login
  description: Acquire (userpass) or confirm (apikey) authentication; caches the token.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: logout
  description: Clear the cached Bearer token for a userpass profile.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
- name: node-delete
  description: 'Delete ONE action from a live process and heal the graph: every port that pointed at it is re-pointed at its successor (or dropped when it has none) -> validate + flow-lint -> PUT. Refuses Start/Stop and a node with more than one outgoing port. --dry-run previews; variables are left alone.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: true
    description: node actionName (canvas label) or id to delete
  - name: dry-run
    type: boolean
    required: false
    description: delete + heal + validate but do not PUT
- name: node-params
  description: List a live process's nodes with each runtime parameter's designer label, current value, editability and bound variables (read-only).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: false
    description: one node's actionName (canvas label) or id; omit for every node
- name: node-replace-text
  description: Replace an EXACT literal in every string leaf of a node's runtime parameters AND designer settings on a live process - the safe way to reach a value nested inside a structured parameter (a Map Data expression, a decisional literal). --expect N asserts the hit count; --dry-run previews. Validates + flow-lints before PUT.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: true
    description: node actionName (canvas label) or id
  - name: property
    type: string
    required: false
    description: narrow the sweep to one parameter (designer label or tabPropertyId)
  - name: find
    type: string
    required: true
    description: exact literal to replace (no regex)
  - name: replace
    type: string
    required: true
    description: replacement literal
  - name: expect
    type: integer
    required: false
    description: assert this many replacements; a mismatch writes nothing
  - name: allow-binding-change
    type: boolean
    required: false
    description: permit a find/replace that touches a positional variable placeholder
  - name: dry-run
    type: boolean
    required: false
    description: replace + normalize + validate but do not PUT
- name: node-set-param
  description: Surgically set ONE node parameter's literal text on a live process (an endpoint, timeout, SQL or script body) -> regenerate the designer layer from the runtime layer -> validate + flow-lint -> PUT. --dry-run to preview; an invalid flow is never PUT. Structured (list/dict) parameters stay process-edit territory.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: true
    description: node actionName (canvas label) or id
  - name: property
    type: string
    required: true
    description: parameter's designer label (e.g. 'Endpoint') or its tabPropertyId
  - name: value
    type: string
    required: true
    description: new literal text; keep every positional variable placeholder the old value had
  - name: allow-binding-change
    type: boolean
    required: false
    description: permit a different placeholder set (only when variable[] is rewritten too)
  - name: dry-run
    type: boolean
    required: false
    description: patch + normalize + validate but do not PUT
- name: patch-actions-folders-rename
  description: 'PATCH /api/Actions/folders/rename  [Actions] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-by-masterworkspaceurl-by-workspaceurl-by-entityurl
  description: PATCH /{masterWorkspaceUrl}/{workspaceUrl}/{entityUrl}  [CustomUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: masterWorkspaceUrl
    type: string
    required: true
    description: path segment {masterWorkspaceUrl}
  - name: workspaceUrl
    type: string
    required: true
    description: path segment {workspaceUrl}
  - name: entityUrl
    type: string
    required: true
    description: path segment {entityUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-by-tinyurl
  description: PATCH /{tinyUrl}  [TinyUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: tinyUrl
    type: string
    required: true
    description: path segment {tinyUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-form-chain-by-chainid-by-forminstanceid
  description: 'PATCH /api/Form/chain/{chainId}/{formInstanceId}  [FormChain] - Permission required: FormInstance.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: chainId
    type: string
    required: true
    description: path segment {chainId}
  - name: formInstanceId
    type: string
    required: true
    description: path segment {formInstanceId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-formapplication-by-id
  description: 'PATCH /api/FormApplication/{id}  [FormApplication] - Permission required: FormTemplate.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: state
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-formtemplate-by-id
  description: 'PATCH /api/FormTemplate/{id}  [FormTemplate] - Permission required: FormTemplate.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: state
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-notifications
  description: 'PATCH /api/Notifications  [Notifications] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-projects-by-id-dataretention
  description: 'PATCH /api/Projects/{id}/dataRetention  [ProcessTemplate] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-projects-by-id-toggle-activation
  description: 'PATCH /api/Projects/{id}/toggle-activation  [ProcessTemplate] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-schedules-by-scheduleid-status
  description: 'PATCH /api/Schedules/{scheduleId}/status  [Schedules] - Permission required: Schedule.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: scheduleId
    type: string
    required: true
    description: path segment {scheduleId}
  - name: enable
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: patch-webhooks-launch-by-id
  description: 'PATCH /api/Webhooks/launch/{id}  [WebhookLaunch] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-actions
  description: 'POST /api/Actions  [Actions] - Permission required: CustomActions.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-actions-event
  description: 'POST /api/Actions/event  [Actions] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-actions-folders
  description: 'POST /api/Actions/folders  [Actions] - Permission required: ProcessDesigner.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-actions-test
  description: 'POST /api/Actions/test  [Actions] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-apikey
  description: 'POST /api/ApiKey  [ApiKey] - Permission required: ApiKey.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-authentication
  description: 'POST /api/Authentication  [Authentication] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-authentication-logout
  description: 'POST /api/Authentication/logOut  [Authentication] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-authentication-refreshtoken
  description: 'POST /api/Authentication/refreshToken  [Authentication] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-by-masterworkspaceurl-by-workspaceurl-by-entityurl
  description: POST /{masterWorkspaceUrl}/{workspaceUrl}/{entityUrl}  [CustomUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: masterWorkspaceUrl
    type: string
    required: true
    description: path segment {masterWorkspaceUrl}
  - name: workspaceUrl
    type: string
    required: true
    description: path segment {workspaceUrl}
  - name: entityUrl
    type: string
    required: true
    description: path segment {entityUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-by-tinyurl
  description: POST /{tinyUrl}  [TinyUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: tinyUrl
    type: string
    required: true
    description: path segment {tinyUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-credentials
  description: 'POST /api/Credentials  [Credentials] - Permission required: Credentials.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-credentials-accesstoken-by-id
  description: 'POST /api/Credentials/accessToken/{id}  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-credentials-test
  description: 'POST /api/Credentials/test  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-credentials-upload-by-credentialsid
  description: 'POST /api/Credentials/upload/{credentialsId}  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: credentialsId
    type: string
    required: true
    description: path segment {credentialsId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-credentials-upload-test
  description: 'POST /api/Credentials/upload/test  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-customurl-formtemplate
  description: 'POST /api/CustomUrl/FormTemplate  [CustomUrlForm] - Permission required: FormTemplate.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-customurl-masterworkspace
  description: 'POST /api/CustomUrl/MasterWorkspace  [CustomUrlMaster] - Permission required: MasterWorkspace.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-customurl-webhook
  description: 'POST /api/CustomUrl/Webhook  [CustomUrlWebhook] - Permission required: Webhook.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-customurl-workspace
  description: 'POST /api/CustomUrl/Workspace  [CustomUrlWorkspace] - Permission required: Workspace.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes
  description: 'POST /api/DataTypes  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-attribute-by-rootdatatypeid
  description: 'POST /api/DataTypes/attribute/{rootDataTypeId}  [DataTypes] - Permission required: DataModels.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: rootDataTypeId
    type: string
    required: true
    description: path segment {rootDataTypeId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-changetopublic
  description: 'POST /api/DataTypes/changeToPublic  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-clone
  description: 'POST /api/DataTypes/clone  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-generate
  description: 'POST /api/DataTypes/generate  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-generate-file
  description: 'POST /api/DataTypes/generate/file  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-datatypes-private
  description: 'POST /api/DataTypes/private  [DataTypes] - Permission required: DataModels.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-debugger-instances-by-id-operation
  description: 'POST /api/Debugger/instances/{id}/operation  [Debugger] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-documenttemplate
  description: 'POST /api/DocumentTemplate  [DocumentTemplate] - Permission required: DocumentDesigner.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-file-upload-action-event
  description: 'POST /api/File/upload/action-event  [File] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-file-upload-flow
  description: 'POST /api/File/upload/flow  [File] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-file-upload-schedule
  description: 'POST /api/File/upload/schedule  [File] - Permission required: Schedule.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-file-upload-testaction
  description: 'POST /api/File/upload/testAction  [File] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-form
  description: 'POST /api/Form  [Form] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-form-upload
  description: 'POST /api/Form/upload  [FileForm] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formapplication
  description: 'POST /api/FormApplication  [FormApplication] - Permission required: FormTemplate.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formprocess-by-formtemplateid-by-flowinstanceid-upload
  description: 'POST /api/FormProcess/{formTemplateId}/{flowInstanceId}/upload  [FileFormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: flowInstanceId
    type: string
    required: true
    description: path segment {flowInstanceId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formprocess-by-formtemplateid-by-processtemplateid-launch
  description: 'POST /api/FormProcess/{formTemplateId}/{processTemplateId}/launch  [FormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: processTemplateId
    type: string
    required: true
    description: path segment {processTemplateId}
  - name: runSynchronous
    type: string
    required: false
    description: query parameter
  - name: secondsTimeOut
    type: string
    required: false
    description: query parameter
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formprocess-by-formtemplateid-by-processtemplateid-publish
  description: 'POST /api/FormProcess/{formTemplateId}/{processTemplateId}/publish  [FormProcess] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: formTemplateId
    type: string
    required: true
    description: path segment {formTemplateId}
  - name: processTemplateId
    type: string
    required: true
    description: path segment {processTemplateId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formtemplate
  description: 'POST /api/FormTemplate  [FormTemplate] - Permission required: FormTemplate.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-formtemplate-by-id-duplicate
  description: 'POST /api/FormTemplate/{id}/duplicate  [FormTemplate] - Permission required: FormTemplate.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects
  description: 'POST /api/Projects  [ProcessTemplate] - Permission required: ProcessDesigner.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-by-id-duplicate
  description: 'POST /api/Projects/{id}/duplicate  [ProcessTemplate] - Permission required: ProcessDesigner.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-by-id-instances-publish
  description: 'POST /api/Projects/{id}/instances/publish  [ProcessInstance] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-by-id-run
  description: 'POST /api/Projects/{id}/run  [ProcessInstance] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: runSynchronous
    type: string
    required: false
    description: query parameter
  - name: secondsTimeOut
    type: string
    required: false
    description: query parameter
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-instances-by-id-launch
  description: 'POST /api/Projects/instances/{id}/launch  [ProcessInstance] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: runSynchronous
    type: string
    required: false
    description: query parameter
  - name: debugMode
    type: string
    required: false
    description: query parameter
  - name: secondsTimeOut
    type: string
    required: false
    description: query parameter
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-instances-by-id-stop
  description: 'POST /api/Projects/instances/{id}/stop  [ProcessInstance] - Permission required: ProcessInstance.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: flowTemplateId
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-notifications
  description: 'POST /api/Projects/notifications  [ProcessTemplate] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-projects-validate
  description: 'POST /api/Projects/validate  [ProcessTemplate] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-schedules
  description: 'POST /api/Schedules  [Schedules] - Permission required: Schedule.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-schedules-notifications
  description: 'POST /api/Schedules/notifications  [Schedules] - Permission required: Schedule.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-subscriptions-refund-by-id
  description: 'POST /api/Subscriptions/refund/{id}  [Subscriptions] - Permission required: MasterWorkspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-subscriptions-renew-by-id-by-state
  description: 'POST /api/Subscriptions/renew/{id}/{state}  [Subscriptions] - Permission required: MasterWorkspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: state
    type: string
    required: true
    description: path segment {state}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-transport-export-entities
  description: 'POST /api/Transport/export-entities  [Transport] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-transport-import
  description: 'POST /api/Transport/import  [Transport] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-userpreferences
  description: 'POST /api/UserPreferences  [UserPreferences] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users
  description: 'POST /api/Users  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-otp-setup
  description: 'POST /api/Users/otp/setup  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-password-change
  description: 'POST /api/Users/password/change  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-password-forgot
  description: 'POST /api/Users/password/forgot  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-password-update
  description: 'POST /api/Users/password/update  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-refer-friend
  description: 'POST /api/Users/refer-friend  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-users-resendtoken
  description: 'POST /api/Users/resendToken  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-webhooks
  description: 'POST /api/Webhooks  [Webhooks] - Permission required: Webhook.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-webhooks-generate-data
  description: 'POST /api/Webhooks/generate-data  [Webhooks] - Permission required: Webhook.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-webhooks-launch-by-id
  description: 'POST /api/Webhooks/launch/{id}  [WebhookLaunch] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-webhooks-listen
  description: 'POST /api/Webhooks/listen  [Webhooks] - Permission required: Webhook.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-workspace
  description: 'POST /api/Workspace  [Workspace] - Permission required: MasterWorkspace.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-workspace-invite
  description: 'POST /api/Workspace/invite  [Workspace] - Permission required: Workspace.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: targetWorkspace
    type: string
    required: false
    description: query parameter
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: post-workspace-transfer-ownership-by-coownerid
  description: 'POST /api/Workspace/transfer-ownership/{coOwnerId}  [Workspace] - Permission required: MasterWorkspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: coOwnerId
    type: string
    required: true
    description: path segment {coOwnerId}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: process-create
  description: Create a PROCESIO process from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: process-delete
  description: Delete a resource by id (DELETE /api/Projects/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
- name: process-edit
  description: Edit a PROCESIO process to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: process-fe-validate
  description: 'Front-end (designer-layer) ''Process Errors'' validation on a live process — the client-side check that BLOCKS designer Save but which POST /api/Projects/validate does NOT catch: unconnected actions, missing Start/Stop, empty node names, required-empty fields, leftover placeholders, bad number/integer values, out-of-range limits, duplicate/primitive-named variables, and unmapped required subprocess inputs. clean=true when no blocking errors. Data-type mismatches are reported as (non-blocking) warnings. This same check auto-runs before every process-create / process-edit. Add --include-be for the BE layer.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id to validate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer
  - name: include-be
    type: boolean
    required: false
    description: also run the back-end validator (POST /api/Projects/validate)
- name: process-toggle-activation
  description: 'Activate or deactivate a process (PATCH /api/Projects/{id}/toggle-activation). --state true|false is REQUIRED: despite its name the endpoint is a setter, and it reads the target from a `state` request header - called without it, it deactivates every time while reporting success. The response says nothing either way, so this reads `active` before and after from the list-processes projection and warns when the result is not what was asked for.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: state
    type: string
    required: true
    description: 'target state: true activates, false deactivates (sent as the `state` header the endpoint reads; it is a setter, not a toggle)'
- name: process-validate
  description: Validate a process with PROCESIO's own validator (POST /api/Projects/validate).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
- name: put-by-masterworkspaceurl-by-workspaceurl-by-entityurl
  description: PUT /{masterWorkspaceUrl}/{workspaceUrl}/{entityUrl}  [CustomUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: masterWorkspaceUrl
    type: string
    required: true
    description: path segment {masterWorkspaceUrl}
  - name: workspaceUrl
    type: string
    required: true
    description: path segment {workspaceUrl}
  - name: entityUrl
    type: string
    required: true
    description: path segment {entityUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-by-tinyurl
  description: PUT /{tinyUrl}  [TinyUrlLaunch]
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: tinyUrl
    type: string
    required: true
    description: path segment {tinyUrl}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-credentials
  description: 'PUT /api/Credentials  [Credentials] - Permission required: Credentials.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-customurl-formtemplate
  description: 'PUT /api/CustomUrl/FormTemplate  [CustomUrlForm] - Permission required: FormTemplate.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-customurl-masterworkspace
  description: 'PUT /api/CustomUrl/MasterWorkspace  [CustomUrlMaster] - Permission required: MasterWorkspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-customurl-webhook
  description: 'PUT /api/CustomUrl/Webhook  [CustomUrlWebhook] - Permission required: Webhook.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-customurl-workspace
  description: 'PUT /api/CustomUrl/Workspace  [CustomUrlWorkspace] - Permission required: Workspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-datatypes
  description: 'PUT /api/DataTypes  [DataTypes] - Permission required: DataModels.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-datatypes-attribute-by-rootdatatypeid
  description: 'PUT /api/DataTypes/attribute/{rootDataTypeId}  [DataTypes] - Permission required: DataModels.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: rootDataTypeId
    type: string
    required: true
    description: path segment {rootDataTypeId}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-debugger-instances-by-id-variables
  description: 'PUT /api/Debugger/instances/{id}/variables  [Debugger] - Permission required: ProcessInstance.Write'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-documenttemplate
  description: 'PUT /api/DocumentTemplate  [DocumentTemplate] - Permission required: DocumentDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-form
  description: 'PUT /api/Form  [Form] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-formapplication
  description: 'PUT /api/FormApplication  [FormApplication] - Permission required: FormTemplate.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-formtemplate
  description: 'PUT /api/FormTemplate  [FormTemplate] - Permission required: FormTemplate.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-projects
  description: Update a process (PUT /api/Projects). Reports HTTP status/elapsed and WARNS on an empty-body success (which can silently not persist).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-projects-put
  description: 'PUT /api/Projects  [ProcessTemplate] - Permission required: ProcessDesigner.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-resourcetrackingconfig-toggle-by-enabled
  description: 'PUT /api/ResourceTrackingConfig/toggle/{enabled}  [ResourceTrackingConfig] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: enabled
    type: string
    required: true
    description: path segment {enabled}
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-schedules
  description: 'PUT /api/Schedules  [Schedules] - Permission required: Schedule.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-userpermissions-by-userid
  description: 'PUT /api/UserPermissions/{userId}  [UserPermissions] - Permission required: Workspace.Admin'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: userId
    type: string
    required: true
    description: path segment {userId}
  - name: targetWorkspace
    type: string
    required: false
    description: query parameter
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-users-details
  description: 'PUT /api/Users/details  [Users] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-webhooks
  description: 'PUT /api/Webhooks  [Webhooks] - Permission required: Webhook.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-webhooks-launch-by-id
  description: 'PUT /api/Webhooks/launch/{id}  [WebhookLaunch] - Permission required: None'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: path segment {id}
  - name: payload
    type: string
    required: false
    description: query parameter
  - name: respondOk
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-workspace
  description: 'PUT /api/Workspace  [Workspace] - Permission required: MasterWorkspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: put-workspace-by-parentid-otp
  description: 'PUT /api/Workspace/{parentId}/otp  [Workspace] - Permission required: MasterWorkspace.Update'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: parentId
    type: string
    required: true
    description: path segment {parentId}
  - name: otpEnabled
    type: string
    required: false
    description: query parameter
  - name: dry-run
    type: boolean
    required: false
    description: compose and return the request without sending it
- name: read-flow-graph
  description: Parse a .procesio export/flow into a node/edge graph model (offline). --resource-map for the cross-process graph.
  args:
  - name: in
    type: string
    required: true
    description: '.procesio export or flow JSON: a path, ''-'' for stdin, or raw JSON'
  - name: flow-id
    type: string
    required: false
    description: which flow when the bundle has more than one
  - name: resource-map
    type: boolean
    required: false
    description: emit every flow + the cross-process (process→process) edge list
- name: relayout-process
  description: 'Re-lay-out a LIVE process in place: read the flow, run the auto-layout engine (LAYOUT_ENGINE env: legacy default / elk), validate, and save back (PUT /api/Projects). --dry-run to preview without saving. Workspace-scoped: pass --workspace-id.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id to re-lay-out in place
  - name: no-validate
    type: boolean
    required: false
    description: skip the POST /api/Projects/validate safety gate before saving
  - name: dry-run
    type: boolean
    required: false
    description: compute + return the new layout WITHOUT saving (no PUT)
  - name: no-cluster
    type: boolean
    required: false
    description: 'legacy engine: disable clustering (clustering is ON by default — the classic engine folds long chains into shelves)'
- name: remove-credential
  description: Delete a credential profile and its cached token.
  args:
  - name: name
    type: string
    required: true
    description: profile name
- name: remove-environment
  description: Remove a user-defined environment (built-in Internal-* cannot be removed).
  args:
  - name: name
    type: string
    required: true
    description: environment name to switch to
- name: rename-actions
  description: 'Rename a LIVE process''s canvas actions in bulk from an id → name map (--map JSON or --map-file), setting BOTH ActionName and CustomData.name; validates, then saves (PUT /api/Projects). Names are cosmetic — wiring is by id — so this cannot break a flow. --dry-run to preview. Workspace-scoped: pass --workspace-id.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id whose actions to rename
  - name: map
    type: string
    required: false
    description: 'id → name map as JSON: {"<action-id>": "New name", ...}. A unique id PREFIX is accepted in place of a full id.'
  - name: map-file
    type: string
    required: false
    description: path to a JSON file holding the same id → name map
  - name: no-validate
    type: boolean
    required: false
    description: skip the POST /api/Projects/validate safety gate before saving
  - name: dry-run
    type: boolean
    required: false
    description: report the renames WITHOUT saving (no PUT)
  - name: force
    type: boolean
    required: false
    description: overwrite even if the resource changed since it was read (skip the optimistic-concurrency check)
- name: repair-datastore-mapper
  description: 'Repair a .procesio pack whose Data Store mapper the export re-spelled into the refused `document`/`process` form (offline). An imported process carrying the exported form cannot run. Idempotent: an already-correct pack is unchanged.'
  args:
  - name: in
    type: string
    required: true
    description: the .procesio pack to repair (a path, or '-' for stdin)
  - name: out
    type: string
    required: false
    description: write the repaired pack here (omit to print it)
  - name: dry-run
    type: boolean
    required: false
    description: report what would change without writing anything
- name: request
  description: 'Call ANY Web-API endpoint: --method --path [--query JSON] [--body JSON].'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: method
    type: string
    required: false
    description: HTTP method (GET/POST/PUT/PATCH/DELETE). Default GET.
  - name: path
    type: string
    required: true
    description: endpoint path, e.g. /api/Projects or /api/Projects/{id}
  - name: query
    type: string
    required: false
    description: query params as a JSON object
  - name: body
    type: string
    required: false
    description: request body as JSON (object or array)
  - name: body-file
    type: string
    required: false
    description: path to a UTF-8 JSON file for the request body (use instead of --body for large payloads like full flow DTOs)
- name: run-form-with-files
  description: 'Submit a form the way a browser does: publish -> upload files -> launch -> read variables, through the public FormProcess endpoints. Proves the form''s own input/output maps carry values.'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: form-id
    type: string
    required: true
    description: form template id
  - name: process-id
    type: string
    required: true
    description: process (flow template) id the form submits to
  - name: file
    type: string
    required: false
    description: VARNAME=PATH for a File input variable (repeatable)
  - name: payload
    type: string
    required: false
    description: non-file input variables as a JSON object
  - name: synchronous
    type: boolean
    required: false
    description: wait for the flow to finish (default)
  - name: no-synchronous
    type: boolean
    required: false
    description: fire and forget
  - name: timeout
    type: integer
    required: false
    description: synchronous wait, seconds (default 120)
- name: run-process
  description: Run a process (POST /api/Projects/{id}/run). Supports --dry-run.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id to run
  - name: payload
    type: string
    required: false
    description: input variables as a JSON object
  - name: connection-id
    type: string
    required: false
    description: connectionid value (default null)
  - name: synchronous
    type: boolean
    required: false
    description: wait for completion (runSynchronous=true)
  - name: timeout
    type: integer
    required: false
    description: secondsTimeOut for synchronous runs
  - name: dry-run
    type: boolean
    required: false
    description: show the request that WOULD be sent; do not run
- name: run-process-with-file
  description: 'Run a process that takes File input variable(s): publish -> upload bytes -> launch -> poll -> output. --file VARNAME=PATH (repeatable).'
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id to run
  - name: file
    type: string
    required: false
    description: bind a local file to a File input variable; repeatable
  - name: payload
    type: string
    required: false
    description: the NON-file input variables as a JSON object
  - name: timeout
    type: integer
    required: false
    description: seconds to wait for a terminal status (default 300)
  - name: poll-interval
    type: number
    required: false
    description: seconds between status polls (default 3)
  - name: no-wait
    type: boolean
    required: false
    description: launch and return the instance id without polling
- name: set-default
  description: Set the default profile used when --profile is omitted.
  args:
  - name: name
    type: string
    required: true
    description: profile name
- name: set-environment
  description: Switch the default environment (e.g. to Internal-QA); persists the choice.
  args:
  - name: name
    type: string
    required: true
    description: environment name to switch to
- name: set-schedule-notifications
  description: Set a schedule's notifications (POST /api/Schedules/notifications) from a JSON --payload.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: notifications config as a JSON object
- name: set-schedule-status
  description: Enable/disable a schedule (PATCH /api/Schedules/{id}/status --active true|false).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: schedule id (scheduleId)
  - name: active
    type: string
    required: true
    description: true to enable the schedule, false to disable it
- name: show-credential
  description: Show one profile's non-secret fields (secrets shown only as has_*).
  args:
  - name: name
    type: string
    required: true
    description: profile name
- name: show-environment
  description: Show one environment's URLs, default flag, and bound credentials.
  args:
  - name: name
    type: string
    required: true
    description: environment name to switch to
- name: sql-convert
  description: Move one SQL node between the Execute Command and Execute Query families (--to) and optionally rebind its Output (--output-variable); validate + flow-lint then PUT (--dry-run to preview). A SELECT-ing procedure on a Command node discards its result set silently - this is the fix.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: true
    description: node actionName or id to convert
  - name: to
    type: string
    required: true
    description: 'target family: query (returns a result set) or command (returns rows affected)'
  - name: output-variable
    type: string
    required: false
    description: flow variable name or id to bind the node's Output to
  - name: dry-run
    type: boolean
    required: false
    description: convert + validate + lint but do not PUT
- name: sql-parameterize
  description: Convert inline N'<%N%>' SQL nodes to safe named-@param binding (--node or --all); validate then PUT (--dry-run to preview).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: node
    type: string
    required: false
    description: node actionName or id to convert
  - name: all
    type: boolean
    required: false
    description: convert every inline SQL node in the process
  - name: dry-run
    type: boolean
    required: false
    description: convert + validate but do not PUT
- name: sql-scan
  description: List a process's SQL Server actions (Execute Query/Command) with inline-vs-parameterized status.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
- name: stop-instance
  description: Stop a running instance (POST /api/Projects/instances/{id}/stop).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: instance id
  - name: flow-template-id
    type: string
    required: false
    description: flowTemplateId (the process id)
- name: update-schedule
  description: Update a schedule (PUT /api/Schedules) from a JSON --payload; --cron/--timezone sets a crontab recurrence.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: payload
    type: string
    required: true
    description: schedule as a JSON object (see PROCESIO-API-NOTES.md for the shape)
  - name: cron
    type: string
    required: false
    description: 5-field crontab expression (min hour day month weekday); sets the recurrence to CRON (type 8) over the --payload. Preview it first with validate-crontab.
  - name: timezone
    type: string
    required: false
    description: IANA timezone id for the cron (e.g. Europe/Bucharest)
  - name: recurrence-info
    type: string
    required: false
    description: human-readable recurrence label (optional)
- name: usage-guide
  description: Regenerate PROCESIO-USAGE-GUIDE.md from the rules the notes in this folder mark with a warning sign (offline, deterministic). The guide carries the rule and a link; the explanation stays in the note, so there is one copy of every fact. It ships to the public repo with the rest of the folder. --check reports staleness without writing.
  args:
  - name: check
    type: boolean
    required: false
    description: Report whether the guide is out of date; write nothing.
  - name: out
    type: string
    required: false
    description: Write somewhere other than the tool folder.
- name: validate-crontab
  description: Preview a crontab expression's next occurrences (POST /api/Schedules/validate-crontab).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: cron
    type: string
    required: true
    description: 5-field crontab expression to validate (min hour day month weekday)
  - name: timezone
    type: string
    required: false
    description: IANA timezone id (default server/UTC)
  - name: count
    type: integer
    required: false
    description: how many upcoming occurrences to return (default 5, max 20)
  - name: start-date
    type: string
    required: false
    description: ISO-8601 activation start (optional)
  - name: end-date
    type: string
    required: false
    description: ISO-8601 activation end (optional)
- name: variable-set-type
  description: Retype one variable of a live process (dataType, optionally isList) -> validate + flow-lint -> PUT. Refuses an input/output variable without --allow-contract-change, because those are the run payload and the response shape callers depend on.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: process (project) id
  - name: variable
    type: string
    required: true
    description: variable name or id
  - name: data-type
    type: string
    required: true
    description: target dataType id (e.g. ...121221 Object, ...121220 Json, ...121214 String)
  - name: is-list
    type: string
    required: false
    description: true/false to also set isList; omit to keep it
  - name: allow-contract-change
    type: boolean
    required: false
    description: permit retyping an input (10) / output (30) variable - changes the public contract
  - name: dry-run
    type: boolean
    required: false
    description: retype + validate but do not PUT
- name: verify-layout
  description: Simulate the designer's rendering of a flow's positions and report problems (container children outside their frame, node overlaps).
  args:
  - name: in
    type: string
    required: true
    description: '.procesio export or flow JSON: a path, ''-'' for stdin, or raw JSON'
  - name: flow-id
    type: string
    required: false
    description: which flow when the bundle has more than one
- name: webhook-create
  description: Create a PROCESIO webhook from a validated config (build->validate->POST->re-GET). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: config
    type: string
    required: false
    description: resource config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build + validate the DTO and return it without creating
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: webhook-delete
  description: Delete a resource by id (DELETE /api/Webhooks/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: webhook id
- name: webhook-edit
  description: Edit a PROCESIO webhook to a desired-state config (--id required). --dry-run to preview the DTO.
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: id of the resource to edit
  - name: config
    type: string
    required: false
    description: desired-state config as a JSON object
  - name: config-file
    type: string
    required: false
    description: path to a JSON config file
  - name: dry-run
    type: boolean
    required: false
    description: build the DTO and return it without editing
  - name: force
    type: boolean
    required: false
    description: bypass the pre-save FE (designer) + BE validation gate
  - name: no-types
    type: boolean
    required: false
    description: skip the advisory data-type-mismatch (warning) layer in the gate
- name: webhook-get
  description: Get one resource by id (GET /api/Webhooks/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: webhook id
- name: webhook-launch
  description: Fire a webhook-triggered process with a payload (POST /api/Webhooks/launch/{id}).
  args:
  - name: profile
    type: string
    required: false
    description: credential profile name (defaults to the stored default profile)
  - name: workspace-id
    type: string
    required: false
    description: workspace GUID to scope this call (sets/overrides the workspaceid header)
  - name: environment
    type: string
    required: false
    description: PROCESIO environment for this call, e.g. Internal-QA (defaults to the credential's binding, else the default environment / Internal-PROD)
  - name: id
    type: string
    required: true
    description: webhook id
  - name: payload
    type: string
    required: false
    description: JSON payload to send to the webhook
